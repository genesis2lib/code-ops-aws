#!/bin/bash

# Status check script for {{module.name}} API Service

echo "=== {{module.name}} API Service Status ==="
echo

# Service status
echo "=== Service Status ==="
if systemctl is-active --quiet {{module.name}}-api.service; then
    echo "✅ Service is RUNNING"
else
    echo "❌ Service is NOT RUNNING"
fi
echo
sudo systemctl status {{module.name}}-api.service --no-pager -l
echo

# Recent logs
echo "=== Recent Logs (Last 20 entries) ==="
sudo journalctl -u {{module.name}}-api.service -n 20 --no-pager
echo

# Log files
echo "=== Application Log Files ==="
if ls /opt/{{module.name}}/logs/{{module.name}}-*.log 1> /dev/null 2>&1; then
    ls -la /opt/{{module.name}}/logs/{{module.name}}-*.log
    echo
    echo "Latest log file (last 10 lines):"
    tail -n 10 /opt/{{module.name}}/logs/{{module.name}}-$(date +%Y%m%d).log 2>/dev/null || echo "No log file for today"
else
    echo "No log files found"
fi
echo

# Current JAR
echo "=== Current Application JAR ==="
if [ -f "/opt/{{module.name}}/lib/app.jar" ]; then
    ls -la /opt/{{module.name}}/lib/app.jar
else
    echo "❌ No JAR file found at /opt/{{module.name}}/lib/app.jar"
fi
echo

# System resources
echo "=== System Resources ==="
echo "Memory usage:"
free -h
echo
echo "Disk usage for /opt/{{module.name}}:"
du -sh /opt/{{module.name}}/* 2>/dev/null || echo "Could not calculate disk usage"
echo

# Service uptime
echo "=== Service Uptime ==="
if systemctl is-active --quiet {{module.name}}-api.service; then
    MAIN_PID=$(systemctl show {{module.name}}-api.service --property=MainPID --value)
    if [ "$MAIN_PID" != "0" ] && [ -n "$MAIN_PID" ]; then
        echo "Service uptime: $(ps -o etime= -p "$MAIN_PID" 2>/dev/null | xargs || echo "Unable to determine")"
    else
        echo "Unable to determine service uptime"
    fi
else
    echo "Service is not running"
fi